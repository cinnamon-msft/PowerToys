<?xml version="1.0" encoding="utf-8" ?>
<winuiex:WindowEx
    x:Class="DoNotDisturb.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winuiex="using:WinUIEx"
    Title="Do Not Disturb"
    Width="420"
    Height="260"
    MinWidth="350"
    MinHeight="200"
    IsMaximizable="True"
    IsMinimizable="True"
    IsResizable="True"
    mc:Ignorable="d">
    <winuiex:WindowEx.SystemBackdrop>
        <MicaBackdrop />
    </winuiex:WindowEx.SystemBackdrop>

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Status Row: Status on left, Auto Mode on right -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Status (small, no card) -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Spacing="8">
                <Ellipse
                    x:Name="StatusIndicator"
                    Width="10"
                    Height="10"
                    VerticalAlignment="Center"
                    Fill="{ThemeResource SystemFillColorCautionBrush}" />
                <TextBlock
                    x:Name="StatusText"
                    VerticalAlignment="Center"
                    FontSize="13"
                    Text="Focus Mode: Off" />
            </StackPanel>

            <!-- Auto Mode Toggle - right aligned -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right" Spacing="4">
                <TextBlock
                    VerticalAlignment="Center"
                    FontSize="13"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="Auto" />
                <ToggleSwitch
                    x:Name="AutoModeToggle"
                    Margin="0,-4,-8,0"
                    VerticalAlignment="Center"
                    OffContent=""
                    OnContent=""
                    Toggled="AutoModeToggle_Toggled" />
            </StackPanel>
        </Grid>

        <!-- Toggle Button -->
        <Button
            x:Name="ToggleButton"
            Grid.Row="1"
            Height="40"
            Margin="0,0,0,12"
            HorizontalAlignment="Stretch"
            Click="ToggleButton_Click"
            Content="Enable Do Not Disturb"
            Style="{ThemeResource AccentButtonStyle}" />

        <!-- Next Meeting Card -->
        <Border
            Grid.Row="2"
            Padding="14"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <FontIcon
                    Grid.Column="0"
                    Margin="0,0,12,0"
                    VerticalAlignment="Center"
                    FontFamily="{ThemeResource SymbolThemeFontFamily}"
                    FontSize="18"
                    Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                    Glyph="&#xE787;" />

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock FontSize="13" FontWeight="SemiBold" Text="Next Meeting" />
                    <TextBlock
                        x:Name="NextMeetingText"
                        FontSize="12"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="No upcoming meetings"
                        TextTrimming="CharacterEllipsis" />
                    <TextBlock
                        x:Name="NextMeetingTime"
                        FontSize="12"
                        Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                        Text="" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</winuiex:WindowEx>
